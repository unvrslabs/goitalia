# Auth Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementare il sistema di autenticazione completo di GoItalIA — registrazione, login email/password, Google OAuth, verifica email, recupero password.

**Architecture:** NextAuth v4 con strategia JWT (nessuna sessione su DB), Prisma Adapter per User/Account, token custom SHA-256 su tabella `AuthToken` separata. Email via Nodemailer SMTP Aruba. Rate limiting in-memory.

**Tech Stack:** Next.js 16 App Router, NextAuth v4, @auth/prisma-adapter, bcryptjs, nodemailer, lru-cache, shadcn/ui

**Spec:** `docs/superpowers/specs/2026-03-22-auth-design.md`

---

## File Map

```
prisma/schema.prisma                              ← aggiungi emailVerified, passwordChangedAt, AuthToken, Account
src/lib/db.ts                                     ← singleton Prisma client
src/lib/tokens.ts                                 ← genera/valida token SHA-256
src/lib/email.ts                                  ← Nodemailer SMTP Aruba
src/lib/auth.ts                                   ← config NextAuth (providers, callbacks, JWT)
src/lib/rate-limit.ts                             ← rate limiter in-memory
src/app/api/auth/[...nextauth]/route.ts           ← handler NextAuth
src/app/api/auth/registrati/route.ts              ← POST registrazione
src/app/api/auth/verifica-email/route.ts          ← POST verifica token
src/app/api/auth/reinvia-verifica/route.ts        ← POST reinvio email verifica
src/app/api/auth/recupera-password/route.ts       ← POST richiesta reset password
src/app/api/auth/nuova-password/route.ts          ← POST salva nuova password
src/middleware.ts                                  ← protezione route + invalidazione sessioni
src/app/(auth)/layout.tsx                         ← layout pagine auth (centrato, sfondo)
src/app/(auth)/login/page.tsx                     ← pagina login
src/app/(auth)/registrati/page.tsx                ← pagina registrazione
src/app/(auth)/controlla-email/page.tsx           ← pagina conferma invio email
src/app/(auth)/verifica-email/page.tsx            ← pagina verifica token (GET)
src/app/(auth)/recupera-password/page.tsx         ← pagina form email reset
src/app/(auth)/nuova-password/page.tsx            ← pagina form nuova password
src/components/auth/login-form.tsx                ← form login (client component)
src/components/auth/registrati-form.tsx           ← form registrazione (client component)
src/components/auth/recupera-password-form.tsx    ← form recupera password (client component)
src/components/auth/nuova-password-form.tsx       ← form nuova password (client component)
```

---

## Task 1: Installa pacchetti + aggiorna schema Prisma

**Files:**
- Modify: `prisma/schema.prisma`
- Create: `src/lib/db.ts`

- [ ] **Step 1: Installa dipendenze**

```bash
cd /Users/emanuelemaccari/goitalia
npm install bcryptjs lru-cache nodemailer
npm install -D @types/bcryptjs @types/nodemailer
```

Atteso: dipendenze installate senza errori

- [ ] **Step 2: Aggiorna schema Prisma**

Modifica `prisma/schema.prisma` — aggiungi/modifica questi modelli (gli altri rimangono invariati):

```prisma
model User {
  id                String    @id @default(uuid())
  email             String    @unique
  name              String?
  passwordHash      String?
  emailVerified     DateTime?
  passwordChangedAt DateTime?
  image             String?
  stripeCustomerId  String?   @unique
  createdAt         DateTime  @default(now())
  updatedAt         DateTime  @updatedAt

  companies    CompanyMember[]
  sessions     Session[]
  accounts     Account[]
  authTokens   AuthToken[]
  agents       Agent[]       // già esistente
  tasks        Task[]        // già esistente
  activityLogs ActivityLog[] // già esistente — RIMUOVI se non esiste ancora
}

// Account OAuth (richiesto da @auth/prisma-adapter per Google)
model Account {
  id                String  @id @default(uuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

// Token per verifica email e reset password
model AuthToken {
  id        String   @id @default(uuid())
  userId    String
  token     String   @unique
  type      String   // "EMAIL_VERIFICATION" | "PASSWORD_RESET"
  expiresAt DateTime
  createdAt DateTime @default(now())

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([token])
  @@index([userId, type])
}
```

**NOTA:** Il modello `Session` già nello schema è per uso custom. NextAuth usa JWT strategy quindi non lo tocca. Lascialo invariato.

- [ ] **Step 3: Rimuovi `activityLogs` da User se causa errori di compilazione**

Se il campo `activityLogs` su User crea problemi (ActivityLog ha agentId, non userId), rimuovilo dalla relazione User.

- [ ] **Step 4: Crea migrazione**

```bash
cd /Users/emanuelemaccari/goitalia
npx prisma migrate dev --name auth-schema
```

Atteso: `✓ Generated Prisma Client` + `Your database is now in sync`

- [ ] **Step 5: Crea `src/lib/db.ts`**

```typescript
import { PrismaClient } from "@prisma/client";

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

export const db =
  globalForPrisma.prisma ??
  new PrismaClient({
    log: process.env.NODE_ENV === "development" ? ["error", "warn"] : ["error"],
  });

if (process.env.NODE_ENV !== "production") globalForPrisma.prisma = db;
```

- [ ] **Step 6: Commit**

```bash
git add prisma/ src/lib/db.ts package.json package-lock.json
git commit -m "feat(auth): schema update + prisma singleton"
```

---

## Task 2: Token utility + Email service + Rate limiter

**Files:**
- Create: `src/lib/tokens.ts`
- Create: `src/lib/email.ts`
- Create: `src/lib/rate-limit.ts`

- [ ] **Step 1: Crea `src/lib/tokens.ts`**

```typescript
import crypto from "crypto";
import { db } from "./db";

export type TokenType = "EMAIL_VERIFICATION" | "PASSWORD_RESET";

/** Genera token raw + salva hash SHA-256 su DB */
export async function createToken(
  userId: string,
  type: TokenType,
  expiresInMs: number
): Promise<string> {
  // Elimina token esistenti dello stesso tipo per questo utente
  await db.authToken.deleteMany({ where: { userId, type } });

  const raw = crypto.randomBytes(32).toString("hex");
  const hash = crypto.createHash("sha256").update(raw).digest("hex");

  await db.authToken.create({
    data: {
      userId,
      token: hash,
      type,
      expiresAt: new Date(Date.now() + expiresInMs),
    },
  });

  return raw; // torna solo il raw, l'hash è nel DB
}

/** Valida token raw — ritorna userId se valido, null altrimenti */
export async function validateToken(
  raw: string,
  type: TokenType
): Promise<string | null> {
  const hash = crypto.createHash("sha256").update(raw).digest("hex");

  const token = await db.authToken.findFirst({
    where: { token: hash, type },
  });

  if (!token) return null;
  if (token.expiresAt < new Date()) {
    await db.authToken.delete({ where: { id: token.id } });
    return null;
  }

  return token.userId;
}

/** Elimina tutti i token di un tipo per un utente */
export async function deleteTokens(
  userId: string,
  type: TokenType
): Promise<void> {
  await db.authToken.deleteMany({ where: { userId, type } });
}
```

- [ ] **Step 2: Aggiungi variabili SMTP a `.env`**

```env
SMTP_HOST="smtps.aruba.it"
SMTP_PORT="465"
SMTP_USER="noreply@goitalia.eu"
SMTP_PASS="..."
SMTP_FROM="GoItalIA <noreply@goitalia.eu>"
```

Chiedi a Emanuele le credenziali SMTP Aruba se non le hai.

- [ ] **Step 3: Crea `src/lib/email.ts`**

```typescript
import nodemailer from "nodemailer";

const transporter = nodemailer.createTransport({
  host: process.env.SMTP_HOST,
  port: Number(process.env.SMTP_PORT),
  secure: true,
  auth: {
    user: process.env.SMTP_USER,
    pass: process.env.SMTP_PASS,
  },
});

const BASE_URL = process.env.NEXTAUTH_URL ?? "http://localhost:3000";

export async function sendVerificationEmail(
  email: string,
  name: string,
  token: string
): Promise<void> {
  const link = `${BASE_URL}/verifica-email?token=${token}`;

  await transporter.sendMail({
    from: process.env.SMTP_FROM,
    to: email,
    subject: "Verifica il tuo account GoItalIA",
    html: `
      <div style="font-family: sans-serif; max-width: 600px; margin: 0 auto;">
        <h2>Ciao ${name}!</h2>
        <p>Clicca il pulsante qui sotto per verificare il tuo account GoItalIA.</p>
        <a href="${link}" style="display:inline-block;padding:12px 24px;background:#16a34a;color:#fff;border-radius:6px;text-decoration:none;font-weight:bold;">
          Verifica account
        </a>
        <p style="color:#6b7280;margin-top:24px;font-size:14px;">
          Il link scade tra 24 ore. Se non hai creato un account, ignora questa email.
        </p>
        <p style="color:#6b7280;font-size:12px;">
          Oppure copia questo link: ${link}
        </p>
      </div>
    `,
  });
}

export async function sendPasswordResetEmail(
  email: string,
  name: string,
  token: string
): Promise<void> {
  const link = `${BASE_URL}/nuova-password?token=${token}`;

  await transporter.sendMail({
    from: process.env.SMTP_FROM,
    to: email,
    subject: "Reimposta la tua password GoItalIA",
    html: `
      <div style="font-family: sans-serif; max-width: 600px; margin: 0 auto;">
        <h2>Ciao ${name}!</h2>
        <p>Hai richiesto di reimpostare la tua password. Clicca il pulsante qui sotto.</p>
        <a href="${link}" style="display:inline-block;padding:12px 24px;background:#16a34a;color:#fff;border-radius:6px;text-decoration:none;font-weight:bold;">
          Reimposta password
        </a>
        <p style="color:#6b7280;margin-top:24px;font-size:14px;">
          Il link scade tra 1 ora. Se non hai richiesto il reset, ignora questa email.
        </p>
      </div>
    `,
  });
}
```

- [ ] **Step 4: Crea `src/lib/rate-limit.ts`**

```typescript
import { LRUCache } from "lru-cache";

type RateLimitOptions = {
  windowMs: number;  // finestra temporale in ms
  max: number;       // max richieste nella finestra
};

export function createRateLimiter(options: RateLimitOptions) {
  const cache = new LRUCache<string, number[]>({
    max: 10000,
    ttl: options.windowMs,
  });

  return function check(key: string): { allowed: boolean; remaining: number } {
    const now = Date.now();
    const windowStart = now - options.windowMs;
    const requests = (cache.get(key) ?? []).filter((t) => t > windowStart);

    if (requests.length >= options.max) {
      return { allowed: false, remaining: 0 };
    }

    requests.push(now);
    cache.set(key, requests);
    return { allowed: true, remaining: options.max - requests.length };
  };
}

// Rate limiter pre-configurati
export const signinLimiter = createRateLimiter({ windowMs: 60_000, max: 5 });
export const passwordResetLimiter = createRateLimiter({ windowMs: 3_600_000, max: 3 });
export const resendVerifyLimiter = createRateLimiter({ windowMs: 3_600_000, max: 3 });
```

- [ ] **Step 5: Commit**

```bash
git add src/lib/
git commit -m "feat(auth): token utils, email service, rate limiter"
```

---

## Task 3: NextAuth config + API route

**Files:**
- Create: `src/lib/auth.ts`
- Create: `src/app/api/auth/[...nextauth]/route.ts`

- [ ] **Step 1: Crea `src/lib/auth.ts`**

```typescript
import { NextAuthOptions } from "next-auth";
import CredentialsProvider from "next-auth/providers/credentials";
import GoogleProvider from "next-auth/providers/google";
import { PrismaAdapter } from "@auth/prisma-adapter";
import bcrypt from "bcryptjs";
import { LRUCache } from "lru-cache";
import { db } from "./db";

// Cache per passwordChangedAt (TTL 5 minuti)
const passwordChangedCache = new LRUCache<string, number | null>({
  max: 5000,
  ttl: 5 * 60 * 1000,
});

export const authOptions: NextAuthOptions = {
  adapter: PrismaAdapter(db) as NextAuthOptions["adapter"],
  session: { strategy: "jwt" },
  pages: {
    signIn: "/login",
    error: "/login",
  },
  providers: [
    GoogleProvider({
      clientId: process.env.GOOGLE_CLIENT_ID!,
      clientSecret: process.env.GOOGLE_CLIENT_SECRET!,
      profile(profile) {
        return {
          id: profile.sub,
          name: profile.name,
          email: profile.email,
          image: profile.picture,
          emailVerified: profile.email_verified ? new Date() : null,
        };
      },
    }),
    CredentialsProvider({
      name: "credentials",
      credentials: {
        email: { label: "Email", type: "email" },
        password: { label: "Password", type: "password" },
      },
      async authorize(credentials) {
        if (!credentials?.email || !credentials?.password) return null;

        const user = await db.user.findUnique({
          where: { email: credentials.email },
        });

        // Dummy compare per prevenire timing attack su email inesistenti
        const dummyHash =
          "$2a$10$dummy.hash.to.prevent.timing.attacks.aaaaaaa";
        const passwordToCheck = user?.passwordHash ?? dummyHash;
        const valid = await bcrypt.compare(credentials.password, passwordToCheck);

        if (!user || !valid) {
          throw new Error("INVALID_CREDENTIALS");
        }

        if (!user.emailVerified) {
          throw new Error("EMAIL_NOT_VERIFIED");
        }

        return {
          id: user.id,
          email: user.email,
          name: user.name,
          passwordChangedAt: user.passwordChangedAt?.getTime() ?? null,
        };
      },
    }),
  ],
  callbacks: {
    async signIn({ user, account, profile }) {
      // Blocca Google OAuth se l'email ha già un account con password
      if (account?.provider === "google") {
        const googleProfile = profile as { email_verified?: boolean };
        if (!googleProfile?.email_verified) return false;

        const existing = await db.user.findUnique({
          where: { email: user.email! },
        });
        if (existing?.passwordHash) {
          return "/login?errore=account-esiste";
        }
      }
      return true;
    },
    async jwt({ token, user, trigger }) {
      // Al primo login, aggiungi dati al token
      if (user) {
        token.id = user.id;
        token.passwordChangedAt =
          (user as { passwordChangedAt?: number | null }).passwordChangedAt ?? null;
      }

      // Controlla invalidazione sessione (con cache 5min)
      if (token.id) {
        const userId = token.id as string;
        const cached = passwordChangedCache.get(userId);

        let dbValue: number | null;
        if (cached === undefined) {
          // Cache miss: leggi dal DB
          const dbUser = await db.user.findUnique({
            where: { id: userId },
            select: { passwordChangedAt: true },
          });
          dbValue = dbUser?.passwordChangedAt?.getTime() ?? null;
          passwordChangedCache.set(userId, dbValue);
        } else {
          dbValue = cached;
        }

        // Se il DB ha un valore più recente del JWT → invalida sessione
        if (dbValue !== null && dbValue !== (token.passwordChangedAt as number | null)) {
          return {}; // token vuoto = sessione invalidata
        }
      }

      return token;
    },
    async session({ session, token }) {
      if (!token.id) return { ...session, user: undefined } as typeof session;
      session.user = {
        ...session.user,
        id: token.id as string,
      };
      return session;
    },
  },
};

// Tipo esteso per la sessione
declare module "next-auth" {
  interface Session {
    user: {
      id: string;
      name?: string | null;
      email?: string | null;
      image?: string | null;
    };
  }
}
```

- [ ] **Step 2: Crea `src/app/api/auth/[...nextauth]/route.ts`**

```typescript
import NextAuth from "next-auth";
import { authOptions } from "@/lib/auth";

const handler = NextAuth(authOptions);
export { handler as GET, handler as POST };
```

- [ ] **Step 3: Commit**

```bash
git add src/lib/auth.ts src/app/api/auth/
git commit -m "feat(auth): nextauth config + api route"
```

---

## Task 4: API routes custom (registrati, verifica, rinvia, reset)

**Files:**
- Create: `src/app/api/auth/registrati/route.ts`
- Create: `src/app/api/auth/verifica-email/route.ts`
- Create: `src/app/api/auth/reinvia-verifica/route.ts`
- Create: `src/app/api/auth/recupera-password/route.ts`
- Create: `src/app/api/auth/nuova-password/route.ts`

- [ ] **Step 1: Crea `src/app/api/auth/registrati/route.ts`**

```typescript
import { NextRequest, NextResponse } from "next/server";
import bcrypt from "bcryptjs";
import { db } from "@/lib/db";
import { createToken } from "@/lib/tokens";
import { sendVerificationEmail } from "@/lib/email";

export async function POST(req: NextRequest) {
  try {
    const { name, email, password } = await req.json();

    if (!name || !email || !password) {
      return NextResponse.json({ error: "Tutti i campi sono obbligatori" }, { status: 400 });
    }

    if (password.length < 8) {
      return NextResponse.json(
        { error: "Password troppo corta (minimo 8 caratteri)" },
        { status: 400 }
      );
    }

    const existing = await db.user.findUnique({ where: { email } });
    if (existing) {
      return NextResponse.json({ error: "Email già registrata" }, { status: 409 });
    }

    const passwordHash = await bcrypt.hash(password, 10);
    const user = await db.user.create({
      data: { name, email, passwordHash },
    });

    const token = await createToken(user.id, "EMAIL_VERIFICATION", 24 * 60 * 60 * 1000);
    await sendVerificationEmail(email, name, token);

    return NextResponse.json({ ok: true });
  } catch {
    return NextResponse.json({ error: "Errore interno" }, { status: 500 });
  }
}
```

- [ ] **Step 2: Crea `src/app/api/auth/verifica-email/route.ts`**

```typescript
import { NextRequest, NextResponse } from "next/server";
import { db } from "@/lib/db";
import { validateToken } from "@/lib/tokens";

export async function POST(req: NextRequest) {
  try {
    const { token } = await req.json();
    if (!token) return NextResponse.json({ error: "Token mancante" }, { status: 400 });

    const userId = await validateToken(token, "EMAIL_VERIFICATION");

    if (!userId) {
      return NextResponse.json(
        { error: "Link non valido o scaduto" },
        { status: 400 }
      );
    }

    await db.user.update({
      where: { id: userId },
      data: { emailVerified: new Date() },
    });

    // Elimina token usato
    import crypto from "crypto";
    const hash = crypto.createHash("sha256").update(token).digest("hex");
    await db.authToken.deleteMany({ where: { token: hash } });

    return NextResponse.json({ ok: true });
  } catch {
    return NextResponse.json({ error: "Errore interno" }, { status: 500 });
  }
}
```

**NOTA:** L'import `crypto` dentro il body è errato — spostalo in cima al file. In implementazione sarà `import crypto from "crypto"` all'inizio.

- [ ] **Step 3: Crea `src/app/api/auth/reinvia-verifica/route.ts`**

```typescript
import { NextRequest, NextResponse } from "next/server";
import { db } from "@/lib/db";
import { createToken } from "@/lib/tokens";
import { sendVerificationEmail } from "@/lib/email";
import { resendVerifyLimiter } from "@/lib/rate-limit";

export async function POST(req: NextRequest) {
  try {
    const { email } = await req.json();
    if (!email) return NextResponse.json({ error: "Email mancante" }, { status: 400 });

    const { allowed } = resendVerifyLimiter(email);
    if (!allowed) {
      return NextResponse.json(
        { error: "Troppi tentativi. Riprova tra un'ora." },
        { status: 429 }
      );
    }

    const user = await db.user.findUnique({ where: { email } });

    // Risposta identica anche se l'utente non esiste (anti-enumerazione)
    if (user && !user.emailVerified) {
      const token = await createToken(user.id, "EMAIL_VERIFICATION", 24 * 60 * 60 * 1000);
      await sendVerificationEmail(email, user.name ?? "utente", token);
    }

    return NextResponse.json({ ok: true });
  } catch {
    return NextResponse.json({ error: "Errore interno" }, { status: 500 });
  }
}
```

- [ ] **Step 4: Crea `src/app/api/auth/recupera-password/route.ts`**

```typescript
import { NextRequest, NextResponse } from "next/server";
import { db } from "@/lib/db";
import { createToken } from "@/lib/tokens";
import { sendPasswordResetEmail } from "@/lib/email";
import { passwordResetLimiter } from "@/lib/rate-limit";

export async function POST(req: NextRequest) {
  try {
    const { email } = await req.json();
    const ip = req.headers.get("x-forwarded-for") ?? "unknown";

    const byIp = passwordResetLimiter(ip);
    const byEmail = passwordResetLimiter(email);

    if (!byIp.allowed || !byEmail.allowed) {
      // Risposta identica per non rivelare se il rate limit è per IP o email
      return NextResponse.json({ ok: true });
    }

    const user = await db.user.findUnique({ where: { email } });

    if (user) {
      const token = await createToken(user.id, "PASSWORD_RESET", 60 * 60 * 1000);
      await sendPasswordResetEmail(email, user.name ?? "utente", token);
    }

    // Risposta identica sempre (anti-enumerazione)
    return NextResponse.json({ ok: true });
  } catch {
    return NextResponse.json({ error: "Errore interno" }, { status: 500 });
  }
}
```

- [ ] **Step 5: Crea `src/app/api/auth/nuova-password/route.ts`**

```typescript
import { NextRequest, NextResponse } from "next/server";
import bcrypt from "bcryptjs";
import { db } from "@/lib/db";
import { validateToken, deleteTokens } from "@/lib/tokens";

export async function POST(req: NextRequest) {
  try {
    const { token, password } = await req.json();

    if (!token || !password) {
      return NextResponse.json({ error: "Dati mancanti" }, { status: 400 });
    }

    if (password.length < 8) {
      return NextResponse.json(
        { error: "Password troppo corta (minimo 8 caratteri)" },
        { status: 400 }
      );
    }

    const userId = await validateToken(token, "PASSWORD_RESET");

    if (!userId) {
      return NextResponse.json(
        { error: "Link non valido o scaduto" },
        { status: 400 }
      );
    }

    const passwordHash = await bcrypt.hash(password, 10);

    await db.user.update({
      where: { id: userId },
      data: {
        passwordHash,
        passwordChangedAt: new Date(),
      },
    });

    // Elimina tutti i token di reset
    await deleteTokens(userId, "PASSWORD_RESET");

    return NextResponse.json({ ok: true });
  } catch {
    return NextResponse.json({ error: "Errore interno" }, { status: 500 });
  }
}
```

- [ ] **Step 6: Fix import crypto in verifica-email/route.ts**

Assicurati che `verifica-email/route.ts` abbia `import crypto from "crypto"` in cima al file, non dentro il body. Refactoring: usa direttamente `validateToken` che già fa l'hash internamente, quindi non serve l'import crypto direttamente nel route — rimuovilo e lascia solo `validateToken` + poi cancella il token con deleteTokens.

Versione corretta di `verifica-email/route.ts`:

```typescript
import { NextRequest, NextResponse } from "next/server";
import { db } from "@/lib/db";
import { validateToken, deleteTokens } from "@/lib/tokens";

export async function POST(req: NextRequest) {
  try {
    const { token } = await req.json();
    if (!token) return NextResponse.json({ error: "Token mancante" }, { status: 400 });

    const userId = await validateToken(token, "EMAIL_VERIFICATION");

    if (!userId) {
      return NextResponse.json(
        { error: "Link non valido o scaduto" },
        { status: 400 }
      );
    }

    await db.user.update({
      where: { id: userId },
      data: { emailVerified: new Date() },
    });

    await deleteTokens(userId, "EMAIL_VERIFICATION");

    return NextResponse.json({ ok: true });
  } catch {
    return NextResponse.json({ error: "Errore interno" }, { status: 500 });
  }
}
```

- [ ] **Step 7: Commit**

```bash
git add src/app/api/auth/
git commit -m "feat(auth): api routes registrazione, verifica, reset password"
```

---

## Task 5: Middleware

**Files:**
- Create: `src/middleware.ts`

- [ ] **Step 1: Crea `src/middleware.ts`**

```typescript
import { withAuth } from "next-auth/middleware";
import { NextResponse } from "next/server";

export default withAuth(
  function middleware(req) {
    // Se il token è vuoto (sessione invalidata da passwordChangedAt), reindirizza al login
    if (!req.nextauth.token?.id) {
      return NextResponse.redirect(new URL("/login?sessione=scaduta", req.url));
    }
    return NextResponse.next();
  },
  {
    callbacks: {
      authorized: ({ token }) => !!token?.id,
    },
  }
);

export const config = {
  matcher: ["/dashboard/:path*", "/onboarding/:path*"],
};
```

- [ ] **Step 2: Commit**

```bash
git add src/middleware.ts
git commit -m "feat(auth): middleware protezione route"
```

---

## Task 6: Layout + Pagine auth

**Files:**
- Create: tutti i file in `src/app/(auth)/` e `src/components/auth/`

- [ ] **Step 1: Installa componenti shadcn necessari**

```bash
cd /Users/emanuelemaccari/goitalia
npx shadcn@latest add input label card badge alert
```

- [ ] **Step 2: Crea `src/app/(auth)/layout.tsx`**

```tsx
export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50 px-4">
      <div className="w-full max-w-md">{children}</div>
    </div>
  );
}
```

- [ ] **Step 3: Crea `src/components/auth/login-form.tsx`**

```tsx
"use client";

import { useState } from "react";
import { signIn } from "next-auth/react";
import { useRouter, useSearchParams } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";

export function LoginForm() {
  const router = useRouter();
  const params = useSearchParams();
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [error, setError] = useState<string | null>(
    params.get("errore") === "account-esiste"
      ? "Questa email è già registrata. Accedi con email e password."
      : params.get("sessione") === "scaduta"
      ? "Sessione scaduta. Accedi di nuovo."
      : null
  );
  const [loading, setLoading] = useState(false);

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    setLoading(true);
    setError(null);

    const res = await signIn("credentials", {
      email,
      password,
      redirect: false,
    });

    if (res?.error === "EMAIL_NOT_VERIFIED") {
      setError("Verifica prima la tua email. Controlla la tua casella di posta.");
    } else if (res?.error) {
      setError("Email o password non corretti");
    } else {
      router.push("/dashboard");
    }

    setLoading(false);
  }

  async function handleGoogle() {
    await signIn("google", { callbackUrl: "/dashboard" });
  }

  return (
    <form onSubmit={handleSubmit} className="space-y-4">
      {error && (
        <div className="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-md text-sm">
          {error}
        </div>
      )}

      <div className="space-y-1">
        <Label htmlFor="email">Email</Label>
        <Input
          id="email"
          type="email"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          placeholder="mario@esempio.it"
          required
        />
      </div>

      <div className="space-y-1">
        <Label htmlFor="password">Password</Label>
        <Input
          id="password"
          type="password"
          value={password}
          onChange={(e) => setPassword(e.target.value)}
          required
        />
        <div className="text-right">
          <a href="/recupera-password" className="text-sm text-green-600 hover:underline">
            Password dimenticata?
          </a>
        </div>
      </div>

      <Button type="submit" className="w-full" disabled={loading}>
        {loading ? "Accesso in corso..." : "Accedi"}
      </Button>

      <div className="relative">
        <div className="absolute inset-0 flex items-center">
          <span className="w-full border-t" />
        </div>
        <div className="relative flex justify-center text-xs uppercase">
          <span className="bg-white px-2 text-gray-500">oppure</span>
        </div>
      </div>

      <Button type="button" variant="outline" className="w-full" onClick={handleGoogle}>
        <svg className="mr-2 h-4 w-4" viewBox="0 0 24 24">
          <path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z" fill="#4285F4"/>
          <path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z" fill="#34A853"/>
          <path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z" fill="#FBBC05"/>
          <path d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z" fill="#EA4335"/>
        </svg>
        Continua con Google
      </Button>

      <p className="text-center text-sm text-gray-600">
        Non hai un account?{" "}
        <a href="/registrati" className="text-green-600 hover:underline font-medium">
          Registrati gratis
        </a>
      </p>
    </form>
  );
}
```

- [ ] **Step 4: Crea `src/app/(auth)/login/page.tsx`**

```tsx
import { Metadata } from "next";
import { LoginForm } from "@/components/auth/login-form";
import { Suspense } from "react";

export const metadata: Metadata = {
  title: "Accedi — GoItalIA",
};

export default function LoginPage() {
  return (
    <div className="space-y-6">
      <div className="text-center">
        <h1 className="text-2xl font-bold text-gray-900">Bentornato</h1>
        <p className="text-gray-600 mt-1">Accedi alla tua azienda AI</p>
      </div>
      <Suspense>
        <LoginForm />
      </Suspense>
    </div>
  );
}
```

- [ ] **Step 5: Crea `src/components/auth/registrati-form.tsx`**

```tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";

export function RegistratiForm() {
  const router = useRouter();
  const [form, setForm] = useState({ name: "", email: "", password: "" });
  const [error, setError] = useState<string | null>(null);
  const [loading, setLoading] = useState(false);

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    setLoading(true);
    setError(null);

    const res = await fetch("/api/auth/registrati", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(form),
    });

    const data = await res.json();

    if (!res.ok) {
      setError(data.error ?? "Errore durante la registrazione");
    } else {
      router.push("/controlla-email");
    }

    setLoading(false);
  }

  return (
    <form onSubmit={handleSubmit} className="space-y-4">
      {error && (
        <div className="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-md text-sm">
          {error}
        </div>
      )}

      <div className="space-y-1">
        <Label htmlFor="name">Nome e cognome</Label>
        <Input
          id="name"
          value={form.name}
          onChange={(e) => setForm({ ...form, name: e.target.value })}
          placeholder="Mario Rossi"
          required
        />
      </div>

      <div className="space-y-1">
        <Label htmlFor="email">Email</Label>
        <Input
          id="email"
          type="email"
          value={form.email}
          onChange={(e) => setForm({ ...form, email: e.target.value })}
          placeholder="mario@azienda.it"
          required
        />
      </div>

      <div className="space-y-1">
        <Label htmlFor="password">Password</Label>
        <Input
          id="password"
          type="password"
          value={form.password}
          onChange={(e) => setForm({ ...form, password: e.target.value })}
          placeholder="Minimo 8 caratteri"
          required
          minLength={8}
        />
      </div>

      <Button type="submit" className="w-full" disabled={loading}>
        {loading ? "Registrazione in corso..." : "Crea account"}
      </Button>

      <p className="text-center text-sm text-gray-600">
        Hai già un account?{" "}
        <a href="/login" className="text-green-600 hover:underline font-medium">
          Accedi
        </a>
      </p>
    </form>
  );
}
```

- [ ] **Step 6: Crea `src/app/(auth)/registrati/page.tsx`**

```tsx
import { Metadata } from "next";
import { RegistratiForm } from "@/components/auth/registrati-form";

export const metadata: Metadata = {
  title: "Registrati — GoItalIA",
};

export default function RegistratiPage() {
  return (
    <div className="space-y-6">
      <div className="text-center">
        <h1 className="text-2xl font-bold text-gray-900">Crea la tua azienda AI</h1>
        <p className="text-gray-600 mt-1">Inizia gratis, nessuna carta di credito</p>
      </div>
      <RegistratiForm />
    </div>
  );
}
```

- [ ] **Step 7: Crea `src/app/(auth)/controlla-email/page.tsx`**

```tsx
import { Metadata } from "next";

export const metadata: Metadata = { title: "Controlla la tua email — GoItalIA" };

export default function ControllaEmailPage() {
  return (
    <div className="text-center space-y-4">
      <div className="text-5xl">📧</div>
      <h1 className="text-2xl font-bold text-gray-900">Controlla la tua email</h1>
      <p className="text-gray-600">
        Ti abbiamo inviato un link di verifica. Clicca il link nell&apos;email per attivare
        il tuo account.
      </p>
      <p className="text-sm text-gray-500">
        Non trovi l&apos;email? Controlla la cartella spam.
      </p>
    </div>
  );
}
```

- [ ] **Step 8: Crea `src/app/(auth)/verifica-email/page.tsx`**

```tsx
"use client";

import { useEffect, useState } from "react";
import { useSearchParams, useRouter } from "next/navigation";
import { Suspense } from "react";

function VerificaEmailContent() {
  const params = useSearchParams();
  const router = useRouter();
  const token = params.get("token");
  const [stato, setStato] = useState<"loading" | "ok" | "error">("loading");

  useEffect(() => {
    if (!token) {
      setStato("error");
      return;
    }

    fetch("/api/auth/verifica-email", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ token }),
    }).then((res) => {
      setStato(res.ok ? "ok" : "error");
      if (res.ok) setTimeout(() => router.push("/login?verificato=1"), 2000);
    });
  }, [token, router]);

  if (stato === "loading") return <p className="text-center text-gray-600">Verifica in corso...</p>;

  if (stato === "ok") return (
    <div className="text-center space-y-3">
      <div className="text-5xl">✅</div>
      <h1 className="text-xl font-bold text-gray-900">Email verificata!</h1>
      <p className="text-gray-600">Reindirizzamento al login...</p>
    </div>
  );

  return (
    <div className="text-center space-y-4">
      <div className="text-5xl">❌</div>
      <h1 className="text-xl font-bold text-gray-900">Link non valido o scaduto</h1>
      <p className="text-gray-600">Il link di verifica non è valido o è scaduto.</p>
      <a href="/login" className="text-green-600 hover:underline text-sm">
        Torna al login e richiedi un nuovo link
      </a>
    </div>
  );
}

export default function VerificaEmailPage() {
  return (
    <Suspense fallback={<p className="text-center">Verifica in corso...</p>}>
      <VerificaEmailContent />
    </Suspense>
  );
}
```

- [ ] **Step 9: Crea form e pagina recupera-password**

`src/components/auth/recupera-password-form.tsx`:

```tsx
"use client";

import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";

export function RecuperaPasswordForm() {
  const [email, setEmail] = useState("");
  const [sent, setSent] = useState(false);
  const [loading, setLoading] = useState(false);

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    setLoading(true);
    await fetch("/api/auth/recupera-password", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ email }),
    });
    setSent(true);
    setLoading(false);
  }

  if (sent) return (
    <div className="text-center space-y-3">
      <div className="text-4xl">📬</div>
      <p className="text-gray-700">
        Se l&apos;email esiste, riceverai un link entro pochi minuti.
      </p>
      <a href="/login" className="text-green-600 hover:underline text-sm">Torna al login</a>
    </div>
  );

  return (
    <form onSubmit={handleSubmit} className="space-y-4">
      <div className="space-y-1">
        <Label htmlFor="email">Email</Label>
        <Input
          id="email"
          type="email"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          placeholder="mario@azienda.it"
          required
        />
      </div>
      <Button type="submit" className="w-full" disabled={loading}>
        {loading ? "Invio in corso..." : "Invia link di recupero"}
      </Button>
      <p className="text-center">
        <a href="/login" className="text-sm text-green-600 hover:underline">Torna al login</a>
      </p>
    </form>
  );
}
```

`src/app/(auth)/recupera-password/page.tsx`:

```tsx
import { Metadata } from "next";
import { RecuperaPasswordForm } from "@/components/auth/recupera-password-form";

export const metadata: Metadata = { title: "Recupera password — GoItalIA" };

export default function RecuperaPasswordPage() {
  return (
    <div className="space-y-6">
      <div className="text-center">
        <h1 className="text-2xl font-bold text-gray-900">Password dimenticata?</h1>
        <p className="text-gray-600 mt-1">Inserisci la tua email per ricevere un link di recupero</p>
      </div>
      <RecuperaPasswordForm />
    </div>
  );
}
```

- [ ] **Step 10: Crea form e pagina nuova-password**

`src/components/auth/nuova-password-form.tsx`:

```tsx
"use client";

import { useState } from "react";
import { useRouter, useSearchParams } from "next/navigation";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";

export function NuovaPasswordForm({ tokenValido }: { tokenValido: boolean }) {
  const router = useRouter();
  const params = useSearchParams();
  const token = params.get("token") ?? "";
  const [password, setPassword] = useState("");
  const [error, setError] = useState<string | null>(null);
  const [loading, setLoading] = useState(false);

  if (!tokenValido) return (
    <div className="text-center space-y-3">
      <div className="text-4xl">❌</div>
      <p className="text-gray-700">Link non valido o scaduto.</p>
      <a href="/recupera-password" className="text-green-600 hover:underline text-sm">
        Richiedi un nuovo link
      </a>
    </div>
  );

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    setLoading(true);
    setError(null);

    const res = await fetch("/api/auth/nuova-password", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ token, password }),
    });

    const data = await res.json();
    if (!res.ok) {
      setError(data.error);
    } else {
      router.push("/login?resetEffettuato=1");
    }
    setLoading(false);
  }

  return (
    <form onSubmit={handleSubmit} className="space-y-4">
      {error && (
        <div className="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-md text-sm">
          {error}
        </div>
      )}
      <div className="space-y-1">
        <Label htmlFor="password">Nuova password</Label>
        <Input
          id="password"
          type="password"
          value={password}
          onChange={(e) => setPassword(e.target.value)}
          placeholder="Minimo 8 caratteri"
          required
          minLength={8}
        />
      </div>
      <Button type="submit" className="w-full" disabled={loading}>
        {loading ? "Salvataggio..." : "Salva nuova password"}
      </Button>
    </form>
  );
}
```

`src/app/(auth)/nuova-password/page.tsx` (Server Component — valida token su GET):

```tsx
import { Metadata } from "next";
import { Suspense } from "react";
import { NuovaPasswordForm } from "@/components/auth/nuova-password-form";
import { validateToken } from "@/lib/tokens";

export const metadata: Metadata = { title: "Nuova password — GoItalIA" };

export default async function NuovaPasswordPage({
  searchParams,
}: {
  searchParams: Promise<{ token?: string }>;
}) {
  const { token } = await searchParams;
  const userId = token ? await validateToken(token, "PASSWORD_RESET") : null;

  return (
    <div className="space-y-6">
      <div className="text-center">
        <h1 className="text-2xl font-bold text-gray-900">Nuova password</h1>
        <p className="text-gray-600 mt-1">Scegli una nuova password sicura</p>
      </div>
      <Suspense>
        <NuovaPasswordForm tokenValido={!!userId} />
      </Suspense>
    </div>
  );
}
```

- [ ] **Step 11: Aggiungi SessionProvider al layout principale**

Modifica `src/app/layout.tsx` per wrappare l'app con SessionProvider:

```tsx
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { SessionProvider } from "next-auth/react";

const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });

export const metadata: Metadata = {
  title: "GoItalIA — La tua azienda AI",
  description: "Crea la tua azienda AI in 5 minuti",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="it" className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">
        <SessionProvider>{children}</SessionProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 12: Crea placeholder `/dashboard`**

```bash
mkdir -p src/app/dashboard
```

`src/app/dashboard/page.tsx`:

```tsx
import { getServerSession } from "next-auth";
import { authOptions } from "@/lib/auth";
import { redirect } from "next/navigation";

export default async function DashboardPage() {
  const session = await getServerSession(authOptions);
  if (!session) redirect("/login");

  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold">Benvenuto, {session.user.name}!</h1>
      <p className="text-gray-600 mt-2">La tua azienda AI è pronta.</p>
    </div>
  );
}
```

- [ ] **Step 13: Build locale per verificare che compila**

```bash
cd /Users/emanuelemaccari/goitalia
npm run build 2>&1 | tail -30
```

Atteso: build senza errori TypeScript

- [ ] **Step 14: Commit**

```bash
git add src/
git commit -m "feat(auth): pagine login, registrati, verifica email, reset password"
```

---

## Task 7: Deploy su VPS

- [ ] **Step 1: Push tutto**

```bash
git push origin main
```

- [ ] **Step 2: Deploy sul VPS**

```bash
ssh root@89.167.3.74 'cd /root/goitalia && git pull && npm install && npm run build && pm2 restart goitalia && echo "DEPLOYED"'
```

- [ ] **Step 3: Aggiungi variabili SMTP al .env sul VPS**

```bash
ssh root@89.167.3.74 'echo "
SMTP_HOST=smtps.aruba.it
SMTP_PORT=465
SMTP_USER=noreply@goitalia.eu
SMTP_PASS=INSERISCI_PASSWORD_SMTP
SMTP_FROM=GoItalIA <noreply@goitalia.eu>
" >> /root/goitalia/.env && pm2 restart goitalia'
```

- [ ] **Step 4: Verifica che le pagine siano raggiungibili**

Apri nel browser:
- `http://89.167.3.74:3002/login`
- `http://89.167.3.74:3002/registrati`

Atteso: pagine visibili e funzionanti

- [ ] **Step 5: Testa flusso completo registrazione**

1. Vai su `/registrati` → compila form → submit
2. Controlla che arrivi email di verifica
3. Clicca link → verifica che account venga attivato
4. Vai su `/login` → accedi → verifica redirect a `/dashboard`
